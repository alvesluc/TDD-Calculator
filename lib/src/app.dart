import 'package:flutter/material.dart';

class TDDCalculator extends StatelessWidget {
  const TDDCalculator({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: 'TDD Calculator',
    );
  }
}
